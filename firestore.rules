rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /savedrecipes/{document=**} {
      allow read, write: if true;  // Temporary for testing (Make secure later)
    }
  }
}
